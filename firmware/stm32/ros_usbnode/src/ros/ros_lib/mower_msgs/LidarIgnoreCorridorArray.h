#ifndef _ROS_mower_msgs_LidarIgnoreCorridorArray_h
#define _ROS_mower_msgs_LidarIgnoreCorridorArray_h

// Auto-generated by firmware/scripts/sync_ros_lib.py
// Do not edit manually — re-run the script after changing .msg files.

#include <stdint.h>
#include <string.h>
#include <stdlib.h>
#include "ros/msg.h"
#include "mower_msgs/LidarIgnoreCorridor.h"
#include "std_msgs/Header.h"

namespace mower_msgs
{

  class LidarIgnoreCorridorArray : public ros::Msg
  {
    public:
      typedef std_msgs::Header _header_type;
      _header_type header;
      uint32_t corridors_length;
      typedef mower_msgs::LidarIgnoreCorridor _corridors_type;
      _corridors_type st_corridors;
      _corridors_type * corridors;

    LidarIgnoreCorridorArray():
      header(),
      corridors_length(0), st_corridors(), corridors(nullptr)
    {
    }

    virtual int serialize(unsigned char *outbuffer) const override
    {
      int offset = 0;
      offset += this->header.serialize(outbuffer + offset);
      *(outbuffer + offset + 0) = (this->corridors_length >> (8 * 0)) & 0xFF;
      *(outbuffer + offset + 1) = (this->corridors_length >> (8 * 1)) & 0xFF;
      *(outbuffer + offset + 2) = (this->corridors_length >> (8 * 2)) & 0xFF;
      *(outbuffer + offset + 3) = (this->corridors_length >> (8 * 3)) & 0xFF;
      offset += sizeof(this->corridors_length);
      for( uint32_t i = 0; i < corridors_length; i++){
        offset += this->corridors[i].serialize(outbuffer + offset);
      }
      return offset;
    }

    virtual int deserialize(unsigned char *inbuffer) override
    {
      int offset = 0;
      offset += this->header.deserialize(inbuffer + offset);
      uint32_t corridors_lengthT = ((uint32_t) (*(inbuffer + offset)));
      corridors_lengthT |= ((uint32_t) (*(inbuffer + offset + 1))) << (8 * 1);
      corridors_lengthT |= ((uint32_t) (*(inbuffer + offset + 2))) << (8 * 2);
      corridors_lengthT |= ((uint32_t) (*(inbuffer + offset + 3))) << (8 * 3);
      offset += sizeof(this->corridors_length);
      if(corridors_lengthT > corridors_length)
        this->corridors = (mower_msgs::LidarIgnoreCorridor*)realloc(this->corridors, corridors_lengthT * sizeof(mower_msgs::LidarIgnoreCorridor));
      corridors_length = corridors_lengthT;
      for( uint32_t i = 0; i < corridors_length; i++){
        offset += this->corridors[i].deserialize(inbuffer + offset);
      }
     return offset;
    }

    virtual const char * getType() override { return "mower_msgs/LidarIgnoreCorridorArray"; };
    virtual const char * getMD5() override { return "83c70620aed7e072a0cf732f7c588afe"; };

  };

}
#endif
